/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package emc.menus.debtors.menuitems.display;

import emc.enums.enumMenuItems;
import emc.forms.debtors.display.allocationimport.DebtorsAllocationImportForm;
import emc.framework.EMCMenuItem;

/**
 *
 * @author riaan
 */
public class DebtorsAllocationImport extends EMCMenuItem {

    /** Creates a new instance of DebtorsAllocationImport */
    public DebtorsAllocationImport() {
        this.setClassPath(DebtorsAllocationImportForm.class.getName());
        this.setMenuItemType(enumMenuItems.ACTION);
        this.setMenuItemName("Allocation Import");
        this.setToolTipText("Import and allocate payments.");
    }
}
