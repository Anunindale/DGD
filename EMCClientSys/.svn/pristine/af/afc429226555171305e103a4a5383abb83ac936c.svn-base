package emc.menus.debtors.menuitems.display;

import emc.enums.enumMenuItems;
import emc.forms.debtors.display.salesregion.DebtorsSalesRegionForm;
import emc.framework.EMCMenuItem;

/**
 *
 * @author wikus
 */
public class DebtorsSalesRegionMenu extends EMCMenuItem {

    /** Creates a new instance of DebtorsCustomerGroupMenu */
    public DebtorsSalesRegionMenu() {
        this.setClassPath(DebtorsSalesRegionForm.class.getName());
        this.setMenuItemType(enumMenuItems.DISPLAY);
        this.setMenuItemName("Sales Region");
        this.setToolTipText("Create and edit sales region");
    }
}
