/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package emc.menus.debtors.menuitems.display;

import emc.enums.enumMenuItems;
import emc.forms.debtors.display.allocationimportfaillog.DebtorsAllocationImportFailLogForm;
import emc.framework.EMCMenuItem;

/**
 *
 * @author riaan
 */
public class DebtorsAllocationImportFailLog extends EMCMenuItem {

    /** Creates a new instance of DebtorsAllocationImportFailLog. */
    public DebtorsAllocationImportFailLog() {
        this.setClassPath(DebtorsAllocationImportFailLogForm.class.getName());
        this.setMenuItemType(enumMenuItems.DISPLAY);
        this.setMenuItemName("Allocation Import Fail Log");
        this.setToolTipText("View Allocation Import Fail Log.");
    }
}
