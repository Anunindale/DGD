/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package emc.forms.sop.display.salesrepcommission.resources;

import emc.app.components.emcJButton;
import emc.enums.enumPersistOptions;
import emc.forms.sop.display.salesrepcommission.SOPSalesRepCommissionForm;
import java.awt.event.ActionEvent;

/**
 *
 * @author wikus
 */
public class SOPsalesRepCommissionButton extends emcJButton {

    private enumPersistOptions type;
    private SOPSalesRepCommissionForm theForm;

    public SOPsalesRepCommissionButton(String val, enumPersistOptions type, SOPSalesRepCommissionForm theForm) {
        super(val);
        this.type = type;
        this.theForm = theForm;
    }

    @Override
    public void doActionPerformed(ActionEvent evt) {
        super.doActionPerformed(evt);
//        List<EMCLookup> lookupList = theForm.getLookupList();
//        List<String[]> tableColumns = theForm.getTableColumns();
//        List<String[]> toGenerate = new ArrayList<String[]>();
//        for (int i = 0; i < lookupList.size(); i++) {
//            toGenerate.add(new String[]{tableColumns.get(i)[0], tableColumns.get(i)[1], (String) lookupList.get(i).getValue()});
//        }
//        new SOPSalesRepCommissionUpdateDialog(type, toGenerate, theForm.getDataRelationManager().getUserData());
//        theForm.getDataRelationManager().refreshData();
    }
}
